# config.py

GITHUB_WEBHOOK_SECRET = "my_webhook_secret"  # Replace with your actual GitHub webhook secret
JSON_FILE_PATH = "prs.json"  # Path to store the pull requests in JSON
PORT = 5000  # Port to run the Flask app
